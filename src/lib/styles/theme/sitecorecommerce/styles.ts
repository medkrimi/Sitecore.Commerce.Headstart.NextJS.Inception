export const styles = {
  // styles for the `body`
  global: {
    body: {
      bg: "bodyBg",
      color: "text",

      fontSize: "sm",

      _dark: {
        color: "textColor.100"
      }
    }
  }
}
